Vagrant.configure(2) do |config|
	config.vm.box = "owenallenaz/sv-kubernetes"
	config.vm.synced_folder ".", "/vagrant", disabled: true

	config.vm.provider "qemu" do |qe|
		qe.arch = "aarch64"
		qe.cpu = "host"
	end
end
